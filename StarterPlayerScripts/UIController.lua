--[[
	UIController
	General UI coordination
	- Show/hide cursor reticle
	- Manage UI visibility per state
]]

local UIController = {}

function UIController.Init(remotesTable, uisTable)
	print("[UIController] Initialized")
	-- Future: manage reticle, UI states
end

return UIController