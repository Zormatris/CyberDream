--[[
	PlayerController
	Player character control
	- Custom walk animation (only while moving)
	- Jump disable
	- Turn lag (future)
]]

local PlayerController = {}

function PlayerController.Init(remotesTable, uisTable)
	print("[PlayerController] Initialized")
	-- Future: configure Humanoid, animations
end

return PlayerController