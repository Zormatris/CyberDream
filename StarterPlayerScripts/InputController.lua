--[[
	InputController
	Handles input routing based on current state
	- ALT mouse toggle
	- RMB zoom (future)
	- F key for calls (future)
	- Space/Click for continue (future)
]]

local InputController = {}

function InputController.Init(remotesTable, uisTable)
	print("[InputController] Initialized")
	-- Future: hook UserInputService
end

return InputController